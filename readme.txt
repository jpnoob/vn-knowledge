my notes about visual novel file formats, with a focus on translating from
japanese to english and have the result be playable

advhd (can change text)
artemis (can change text)
bgi (can change text and images, don't know how to change system images/text)
catsystem2 (can change text)
kirikiri (can change text and images (depending on subformat), decrypter must exist)
musica (can change text and images, depacker must exist)
pajamas (can change text)
silky (can change text)
softpal (can change text and images, string pointers are a bit iffy)
willadv (can change text)
yuka system (can change text)

yu-ris (i only took a brief look, but it looks cumbersome, some tools that don't work and lots of stuff without documentation)
malie (i only took a very brief look, but it looks very tricky)
